version: "3.8"

services:
  backend:
    build: ./Backend/
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
  website:
    build: ./website/
    ports:
      - "80:8000"
    depends_on:
      - backend
