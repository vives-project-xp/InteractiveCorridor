export default class PingTimer {
    private keepalive;
    private timer;
    private checkPing;
    private _setTimeout;
    private _clearTimeout;
    constructor(keepalive: number, checkPing: () => void);
    clear(): void;
    reschedule(): void;
}
